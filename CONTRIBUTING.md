# Contributing to AfiuCMS

AfiuCMS is currently an early alpha. Keep pull requests focused, use descriptive commit messages and avoid committing secrets or local `.env` files.

For new features, prefer a dedicated branch such as `feature/theme-customizer` or `fix/media-validation`. Run PHP syntax checks before opening a pull request.
